# Reload ZSH
alias reload!='. ~/.zshrc'
# Shortcut to create a screenshot of a website
alias snapweb="webkit2png --datestamp --ignore-ssl-check --dir=$SCREENSHOT_DIR"
# Good 'ol Clear Screen command
alias cls='clear'
