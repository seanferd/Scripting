<?php

//$xpath = new DOMXPath( $dom );

//echo( htmlspecialchars( $dom->saveXML() ) );

//$elements = $xpath->query( "///div[@id='body-copy']" );
//$elements = $xpath->query( "///title" );


//follow this route, add all of the queries and then we'll parse them
//into variables and dump it on GC.. this is taking too long
// $arrSearch = array( $xpath->query( "//*[@id='subtitle']" ) );
// $elements = $xpath->query( "//*[@id='subtitle']" );
// $elements = $xpath->query( "//title" );
// //$elements = $xpath->query( "//meta[@name='description']" and content? );
// $elements = $xpath->query( "//meta[@name='description']" );
//
//
// foreach( $elements as $e ) {
//   //var_dump( $e );
//   echo $e->nodeValue  . "<br/>";
//   //var_dump( $e->attributes );
// }
// //echo( htmlspecialchars( $body ) );
//
// //echo( preg_match( "/virtual/i", $body ) );
//
// $divs = $dom->getElementsByTagName('*');
// //$divs = $dom->getElementById('body-copy');
//
// foreach ($divs as $div) {
// //echo $div->ownerDocument->saveHTML($divs->childNodes);
//     //debug( $div );
// //var_dump( $div->attributes );
//
// //echo $div->nodeName;
//
// //echo $div->nodeValue;
//      foreach ($div->attributes as $attr) {
//        $name = $attr->nodeName;
//        $value = $attr->nodeValue;
//     //   //echo( $attr->parentNodes->nodeValue );
//        //echo "Attribute '$name' :: '$value'<br />";
//
//        if( $value == "headline" ) {
//          //echo "<h1>" . $attr->nodeValue . "</h1>";
//          //var_dump( $attr->lastChild->textContent );
//        }
//     //
//     //   foreach( $divs as $child ) {
//     //     //echo $child->ownerDocument->saveHTML( $child );
//     //   }
//      }
//     //var_dump( $div->)
// }

// $valueID = $searchMeta->getAttribute('property');
//
// $xmlDate = $searchMeta->getElementsByTagName( "Date" );
// $valueDate = $xmlDate->item(0)->nodeValue;
//
// $xmlAuthorID = $searchMeta->getElementsByTagName( "AuthorID" );
// $valueAuthorID = $xmlAuthorID->item(0)->nodeValue;
//
// echo "$valueID - $valueDate - $valueAuthorID\n" . "<br/>";

?>
